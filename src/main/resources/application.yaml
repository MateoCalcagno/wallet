spring:
  application:
    name: wallet

  datasource:
    url: jdbc:postgresql://localhost:5432/wallet-db?currentSchema=public&stringtype=unspecified&reWriteBatchedInserts=true
    username: mateo
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        jdbc:
            time_zone: UTC
        format_sql: true

server:
  port: 8080